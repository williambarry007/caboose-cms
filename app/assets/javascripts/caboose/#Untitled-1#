<%
image_url = block.field_value('image_url')
text = block.field_value('text')

%><% if image_url && text %>
<div class="logo"><img src="<%= raw image_url %>" alt="<%= text %>" /></div>
<div class="name"><%= text %></div>
<% else %>
<%= empty_text %>
<% end %>
